{
  "version": 2,
  "buildCommand": "pnpm --filter=client build && test -d client/dist && mv client/dist dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "dist",
  "framework": "vite"
}
